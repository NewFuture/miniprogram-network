{
  "name": "miniprogram-network-life-cycle",
  "version": "2.1.0",
  "description": "miniprogram-network",
  "main": "dist/index.js",
  "types": "types/index.d.ts",
  "files": [
    "dist/**",
    "types/**",
    "src/"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist && rm -rf types",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "miniprogram-cancel-token": "^0.2.2"
  },
  "author": "New Future",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/NewFuture/miniprogram-network.git"
  }
}
