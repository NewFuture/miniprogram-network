{
  "name": "miniprogram-network-life-cycle",
  "version": "3.1.0-alpha.0",
  "description": "miniprogram-network",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/NewFuture/miniprogram-network.git"
  },
  "license": "Apache-2.0",
  "author": "New Future",
  "files": [
    "dist/**",
    "types/**",
    "src/"
  ],
  "main": "dist/index.js",
	"module": "es/index.js",
	"types": "types/index.d.ts",
	"scripts": {
		"build": "npm run build:es5 && npm run build:es6",
		"build:es5": "tsc --declarationDir types",
		"build:es6": "tsc -m es6 --outDir es",
    "clean": "rm -rf dist && rm -rf types",
    "lint": "tslint -p . -c ../tslint.json",
    "lint:fix": "tslint --fix -p . -c ../tslint.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "miniprogram-cancel-token": "^0.4.0-alpha.0"
  }
}
